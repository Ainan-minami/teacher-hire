# 开发决策日志（DECISIONS.md）

本文件记录项目开发过程中的关键决策与理由，方便后续迭代时回溯"为什么这么做"。

## 1. 技术栈总览

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 爬虫语言 | Python 3.10+ | 生态成熟、BeautifulSoup 解析 HTML 简单；GitHub Actions 免费支持 |
| HTTP 层 | urllib 标准库封装 | 避免 requests 依赖；本地/CI 行为一致 |
| 解析库 | BeautifulSoup4 + lxml | 稳定、轻量；lxml 用于 RSS/XML 解析更可靠 |
| 存储 | 仓库内静态 JSON | 日增约 200-300 条（~100KB），GitHub Pages 直接托管；无数据库配额风险、版本可回溯 |
| 前端 | 原生 HTML/CSS/JS | 无构建步骤、无框架体积，移动端首屏快；加功能直接改 JS |
| 地图 | Leaflet + OSM 瓦片 | 免费、无需 Key；国内 CDN 加载 |
| 部署 | GitHub Pages（Actions artifact） | 免费、支持子路径、无需额外配置 |
| 定时 | GitHub Actions cron（07:00 UTC） | 每月 2000 分钟免费，单次任务约 5-8 分钟 |

## 2. 数据源选择（核心决策）

### 2.1 万行教师人才网（job910）— 主力民办校来源

- **方案**：搜索列表页 `job/search?jobType=30&pageIndex=N`，列表卡片自带薪资/学历/经验/城市/更新时间，**不抓详情页**。
- **理由**：详情页还需额外请求且部分字段藏在 JS 里；列表页已满足 90% 需求。经实测 `pageIndex` 是唯一生效的分页参数。
- **风险与对策**：列表按"更新于"排序，部分旧职位可能长期置顶；通过 `max_age_days=120` 清理过期记录。

### 2.2 教育部人才服务网（jybzp）— 权威官方来源

- **方案**：列表为表单 POST 分页（hidden `start` 偏移），每页 10 条；抓取直属单位 + 高校两个分类。
- **理由**：教育部直属单位与高校招聘信息权威、链接稳定。
- **实测发现**：`?page=2` 参数无效，必须 POST 提交 `start=10` 才能翻页（已在代码注释中记录）。

### 2.3 应届生招聘网（yjszp）— 公告流补充

- **方案**：首页公告流 `?page=N` 分页，每页约 30 条；按教师相关关键词过滤后入库。
- **理由**：该站汇聚大量高校、事业单位、中小学教师公告，静态 HTML 结构稳定，无需登录。

### 2.4 主动放弃的渠道（决策记录）

| 渠道 | 放弃原因 |
|------|----------|
| 高校人才网 gaoxiaojob | 403 反爬；GitHub Actions 海外 IP 更难访问 |
| 全国事业单位招聘网 qgsydw | JS 动态渲染 + 导航混乱；抓取成本高且产出偏事业单位 |
| 必应 RSS 搜索聚合 | 中文招聘查询返回百科/门户噪音，相关率极低（实测多次）；不符合"能稳定输出结构化数据"标准 |
| 智联/BOSS/前程无忧 | 反爬严格，违背零成本原则 |
| 万行之外的小站（jszp.cc 等） | 空壳/证书问题/内容质量差 |

## 3. 字段提取策略（规则优先，暂不用大模型）

- **选择**：正则 + 关键词词典（省份/城市/学历/学科/学段/经验/薪资/截止日期）。
- **理由**：零 API 成本、无延迟、结果可复现；当前数据量下规则已能达到 85% 省份覆盖率。
- **后续**：若需更高精度，可引入 DeepSeek/Gemini 免费额度做二次校验，但设计上保持"规则兜底"。

## 4. 去重与数据生命周期

- 主键 = URL 的 md5；同 URL 保留最新抓取记录（用 `crawl_date` 判定新旧）。
- 保留最近 120 天记录；每日快照保留 14 天，避免仓库无限膨胀。
- 已删除数据源的历史记录会被自动剔除（`runner` 中按 `source` 过滤）。

## 5. 部署与自动化

- 单工作流三件事：crawl（跑爬虫+提交）→ deploy（Pages）。
- `concurrency` 防止定时任务与手动触发重叠。
- 前端数据用相对路径加载，仓库在根路径或项目子路径下都能正常访问。
- 不需要任何 Secret；新增数据源如需 Cookie，在 workflow 中留了 `env` 注入位（见 config 注释）。

## 6. 已知局限与后续路线

- 覆盖渠道目前约 15-20%（按主流发布渠道估算），优先保证稳定性；后续按 README 路线接入省市级人事考试网。
- 求职者需点击"查看原文"核实最新状态（聚合站天然存在信息滞后）。
- 若数据量增长到单文件 >2MB，再引入 Supabase 或分片索引。
